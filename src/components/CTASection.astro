---
const { title, desc, btnText } = Astro.props;
---

<section id="contact" class="mt-24 text-center section-padding bg-gradient-to-r from-skycare-light to-white rounded-3xl shadow-sm">
  <h3 class="text-2xl sm:text-3xl font-bold text-skycare-darkblue mb-4">{title}</h3>
  <p class="text-gray-600 mb-8 text-sm sm:text-base">{desc}</p>
  <a href="tel:010-3201-0019" class="btn-skycare px-8 py-4 text-base sm:text-lg w-full sm:w-auto">{btnText || "전화 문의"}</a>
</section>

